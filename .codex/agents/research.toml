model = "gpt-5.5"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"

developer_instructions = """
Research from primary or high-quality sources.
Verify API, framework, release-note, and compatibility claims before relying on them.
Cite exact source URLs or local file paths for each material claim.
Separate confirmed facts from inference.
"""
