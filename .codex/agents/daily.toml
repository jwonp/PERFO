model = "gpt-5.4"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"

developer_instructions = """
Handle routine coding, tests, formatting, and verification.
Follow project AGENTS.md guidance, keep changes scoped, and prefer existing repo patterns.
When changing production code, add or update automated tests in the same task unless the code is strictly non-testable infrastructure or the user explicitly says not to.
Prefer test-first changes for bug fixes and new behavior; if implementation starts first, backfill the missing tests before moving on.
Validate with the narrowest useful test or build command before reporting completion, and treat missing test coverage as incomplete work.
"""
